name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-flask pytest-cov pytest-mock

      - name: Lint with flake8
        run: flake8 ai_bag_agent/ tests/ --max-line-length=88 --extend-ignore=E203

      - name: Format check with black
        run: black --check ai_bag_agent/ tests/

      - name: Import order check with isort
        run: isort --check-only ai_bag_agent/ tests/

      - name: Run tests
        env:
          FLASK_ENV: testing
          FLASK_SECRET_KEY: test-secret-key-for-ci
          DATABASE_URL: sqlite:///test.db
        run: |
          python -m pytest tests/ -v --cov=ai_bag_agent --cov-report=xml --cov-fail-under=70

      - name: Upload coverage
        uses: codecov/codecov-action@v4
        if: matrix.python-version == '3.11'
        with:
          files: coverage.xml
          fail_ci_if_error: false
