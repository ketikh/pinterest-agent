# Development / test dependencies. Production deploys don't need these.
# Install on dev box: pip install -r requirements-dev.txt
#
# pip-audit ignores this file because the runtime production environment
# never installs pytest et al.

-r requirements.txt

# Pin pytest to 8.x — pytest-asyncio 1.x doesn't yet support pytest 9.x.
pytest>=8.4.2,<9
pytest-asyncio>=1.3.0
pytest-mock>=3.15.1
